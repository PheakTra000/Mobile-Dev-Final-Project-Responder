Download your real service account key from:
Firebase Console > Project Settings > Service Accounts > Generate new private key

Save it here as serviceAccountKey.json (this exact filename is what
firebase_config.js loads by default via GOOGLE_APPLICATION_CREDENTIALS).

NEVER commit the real serviceAccountKey.json to git — add it to .gitignore.
